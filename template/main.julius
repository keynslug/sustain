jQuery(function () {
    $('.pkglist li div.btn').tooltip();
    $('.navsidebar').affix({
        offset: {
            top: function () {
                var el = $('#header')
                return (this.top = el.outerHeight() * 2)
            }
        }
    });
});
